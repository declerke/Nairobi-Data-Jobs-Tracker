# Extra dependencies for the Airflow image.
# Airflow 2.8.4 already provides: SQLAlchemy, Jinja2, psycopg2, cryptography,
# python-dateutil, pytz, requests, urllib3, tenacity — so those are omitted
# or left unpinned to avoid resolver conflicts.

# Scraping
beautifulsoup4==4.12.3
lxml
html5lib==1.1
httpx==0.26.0
fake-useragent==2.2.0
playwright==1.58.0

# Utilities
python-dotenv==1.0.1
colorlog==6.8.2
python-json-logger==2.0.7
tqdm==4.66.1
unidecode==1.3.8
ujson==5.9.0
email-validator==2.1.0
